# Server Configuration
PORT=3000

# ⚠️ Railway MySQL Database (REQUIRED - No local database fallback)
DB_HOST=tramway.proxy.rlwy.net
DB_PORT=28820
DB_USER=root
DB_PASSWORD=qbQIgmWhiBpdxWUYWMNYBYoqCpcEKFkX
DB_NAME=railway

# NOTE: Using localhost will cause the app to FAIL intentionally
# This ensures all data is stored in Railway MySQL only
